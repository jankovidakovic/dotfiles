- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.config/nvim: nvim
    ~/.config/zsh: zsh
    ~/.config/tmux: tmux
    ~/.config/sway: sway
    ~/.config/foot: foot
    ~/.gitconfig: git/.gitconfig
