- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.config/nvim: nvim
    ~/.config/zsh: zsh
    ~/.config/tmux: tmux
    ~/.config/sway: sway
    # wayland overlay bar
    ~/.config/wob.ini: sway/other_configs/wob.ini
    ~/.config/foot: foot
    ~/.gitconfig: git/.gitconfig
