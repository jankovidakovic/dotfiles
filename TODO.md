# extract nvim stuff to `plugin/`

the `plugin/` dir gets sourced automatically in nvim.
Currently almost everything is in `lua/plugins/...`, which gets
sourced by Lazy.

I wanna go over the code and see what stuff belongs
in `plugin/` (stuff that we wanna include automatically).


